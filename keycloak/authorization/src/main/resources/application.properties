# OIDC Configuration
quarkus.oidc.auth-server-url=http://localhost:8080/realms/demo
quarkus.oidc.client-id=authorization-demo
quarkus.oidc.credentials.secret=authorization-demo-secret
quarkus.oidc.application-type=web-app
quarkus.oidc.authentication.redirect-path=/
quarkus.oidc.authentication.restore-path-after-redirect=true
quarkus.oidc.logout.path=/logout
quarkus.oidc.logout.post-logout-path=/
quarkus.oidc.roles.source=accesstoken
quarkus.oidc.token.principal-claim=preferred_username

# Keycloak Admin Client
quarkus.keycloak.admin-client.server-url=http://localhost:8080
quarkus.keycloak.admin-client.realm=demo
quarkus.keycloak.admin-client.client-id=authorization-demo
quarkus.keycloak.admin-client.client-secret=authorization-demo-secret
quarkus.keycloak.admin-client.grant-type=CLIENT_CREDENTIALS

# Keycloak Policy Enforcer
quarkus.keycloak.policy-enforcer.enabled=true
quarkus.keycloak.policy-enforcer.lazy-load-paths=false

# Public paths - no authorization required
quarkus.keycloak.policy-enforcer.paths.root.paths=/
quarkus.keycloak.policy-enforcer.paths.root.enforcement-mode=disabled
quarkus.http.auth.permission.public.paths=/
quarkus.http.auth.permission.public.policy=permit

# Claim Information Point - pass request path to Keycloak for policy evaluation
quarkus.keycloak.policy-enforcer.paths.user.paths=/users/*
quarkus.keycloak.policy-enforcer.paths.user.claim-information-point.claims.request-uri={request.relativePath}

# HTTP
quarkus.http.port=8081

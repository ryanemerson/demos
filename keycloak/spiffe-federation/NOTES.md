# Notes

## SPIRE
- Should we be using the SPIFFE oidc-discover-provider to expose the JWKS endpoint?
  - The federation bundle endpoint is intended for federation, not discovery
  - https://github.com/spiffe/spire/pkgs/container/oidc-discovery-provider
